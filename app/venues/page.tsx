import Link from 'next/link'; import Header from '@/components/Header'; import VenueCard from '@/components/VenueCard'; import {sessions,venues} from '@/lib/conference';
export default function Venues(){return <main className="shell detail-shell"><Header compact/><Link href="/?tab=more" className="back">← 더보기</Link><div className="section-heading"><div><span className="kicker">VENUE GUIDE</span><h1>발표장 안내</h1></div><strong>{venues.length}</strong></div><div className="list">{venues.map(v=><VenueCard key={v.id} venue={v} sessionCount={sessions.filter(s=>s.venue===v.name).length}/>)}</div></main>}
